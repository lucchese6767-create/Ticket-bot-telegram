services:
  - type: web
    name: telegram-ticket-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SUPPORT_GROUP_ID
        sync: false
